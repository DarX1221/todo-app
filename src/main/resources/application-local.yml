spring:
  h2:
    console:
      enabled: true
      path: '/console'
  datasource:
    url: 'jdbc:h2:file:/home/dark/IdeaProjects/Spring-Study/todo-app/todo-app/todoDB'
    driverClassName: 'org.h2.Driver'
    username: 'sa'
  jpa:
    database-platform: 'org.hibernate.dialect.H2Dialect'
    hibernate:
      ddl-auto: validate
management:
  endpoints:
    web:
      exposure:
        include: '*'



